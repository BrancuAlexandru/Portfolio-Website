import React, { useEffect } from 'react';

const ProjectMenuSection = (props) => {
  useEffect(() => {
    if (props.activeSection === 'photos') {
      
    }
  }, [props.activeSection])
  return (
    ''
  )
}

export default ProjectMenuSection;